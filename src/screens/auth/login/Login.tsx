import React, { FC } from 'react';
import { Text } from 'atoms';
import { Center } from 'layouts';

const Login: FC = () => {
  return (
    <Center white>
      <Text>Login</Text>
    </Center>
  );
};

export default Login;